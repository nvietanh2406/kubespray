USER=datxadmin; PASSWORD="eddde262-b4a5-4a20-8bdb-3b7f0984c836"; echo "${USER}:$(openssl passwd -stdin -apr1 <<< ${PASSWORD})" >> auth

kubectl -n longhorn-system create secret generic basic-auth --from-file=auth
